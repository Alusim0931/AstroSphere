{% extends 'base.html.twig' %}

{% block title %}Empleado{% endblock %}

{% block body %}
    <h1>Empleado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ empleado.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ empleado.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ empleado.apellidos }}</td>
            </tr>
            <tr>
                <th>Foto</th>
                <td>{{ empleado.foto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_empleado_index') }}">back to list</a>


    {{ include('empleado/_delete_form.html.twig') }}
{% endblock %}
